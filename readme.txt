
## PD Litmus: a 'semantic' coloring module

This is a little text filter that puts colored spans around nonterminal words within <code> tags. Eventually intended as a plugin for PDistillery. Also useful as a standalone library to color your self-hosted code.